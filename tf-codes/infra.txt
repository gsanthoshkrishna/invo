The plan is to create a k8s cluster with one node which contains both master and worker node on same vm.
Write a tf code to create an ec2 instance with a1.xlarge.
there seems tobe issue with vm when running terraform.
try using aws cli. write a script to create ec2 instance with tag name 
and also delete the vm by running the shell script put this deleting vm in crontab.
